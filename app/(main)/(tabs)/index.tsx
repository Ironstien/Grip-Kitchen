import { ScreenPlaceholder } from '@/components/screens/ScreenPlaceholder';

export default function TodayScreen() {
  return (
    <ScreenPlaceholder
      title="Today"
      description="Your meals and expiring items will appear here."
      actionLabel="Plan a meal"
    />
  );
}
