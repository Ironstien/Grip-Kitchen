import { ScreenPlaceholder } from '@/components/screens/ScreenPlaceholder';

export default function PlannerScreen() {
  return (
    <ScreenPlaceholder
      title="Planner"
      description="Schedule meals across month, week, and agenda views."
      actionLabel="Plan a meal"
    />
  );
}
